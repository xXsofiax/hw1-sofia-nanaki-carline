"use strict";
let population = new Array(2);
let totalpop;
let averOfreps;
function readCSV(){
    //read the excel file and sign the data in population array.
}

function totalPo(){
    //to add all the population together
    for(let i = 0; i<population[1].length;i++){
        totalpop += population[1][i];
    }
    return total;
}

function calculateReps(){
    // to calculate the average of population
    averOfreps = totalpop / population[1].length;
    return averOfreps;
}

